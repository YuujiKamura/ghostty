warning: function `get_staged_diff` is never used
  --> src\git.rs:52:8
   |
52 | pub fn get_staged_diff(repo_dir: &Path) -> Option<String> {
   |        ^^^^^^^^^^^^^^^
   |
   = note: `#[warn(dead_code)]` (part of `#[warn(unused)]`) on by default

warning: function `get_staged_files` is never used
  --> src\git.rs:69:8
   |
69 | pub fn get_staged_files(repo_dir: &Path) -> Vec<PathBuf> {
   |        ^^^^^^^^^^^^^^^^

warning: `ai-code-review` (lib) generated 2 warnings
    Finished `dev` profile [unoptimized + debuginfo] target(s) in 0.29s
     Running `target\debug\review.exe --analyze 'C:\Users\yuuji\ghostty-win\src\apprt\winui3\App.zig' --backend gemini --prompt architecture --context`
## Analyze: App.zig

GhosttyのWinUI 3ランタイムにおける核心部である`App.zig`の分析結果を簡潔にまとめます。

### 1. 目的と責務
*   **SDKブートストラップ**: Windows App SDKの初期化とWinRTランタイムの起動。
*   **ウィンドウ・UI管理**: WinUI 3ウィンドウの生成、HWNDサブクラス化によるメッセージルーティング、`TabView`を用いたマルチタブの管理。
*   **ライフサイクル制御**: アプリの起動（`Application.Start`）、イベントループ、終了処理、リソースのクリーンアップのオーケストレーション。
*   **ブリッジング**: Ghosttyコア（`CoreApp`）とWindowsネイティブUI（WinUI 3/Win32）間のアクション（`performAction`）および入力の仲介。

### 2. 依存関係
*   **内部 (Ghostty Core)**: `../../App.zig`, `../../Surface.zig`, `../../config.zig` 等に依存し、Ghosttyの共通ロジックと接続。
*   **ランタイムモジュール**: `window_runtime`, `tabview_runtime`, `input_runtime` 等の専門化されたヘルパー群を使用。
*   **外部 (Windows/WinRT)**: `Windows App SDK`, `winrt.zig`, `com.zig` を介してCOM/WinRT APIを広範に使用。
*   **被使用**: `apprt.zig`（プラットフォーム抽象化層）から呼び出される具象実装。

### 3. 設計パターン
*   **COM Aggregation**: `AppOuter`を使用して`IXamlMetadataProvider`を実装。WinUI 3のカスタムコントロールに必要なメタデータを注入するWinRTの標準パターン。
*   **Facade / Orchestrator**: 複雑なWinRT初期化手順やサブシステム（タブ、ウィンドウ、入力）を統合し、簡素なAPI（`init`, `run`, `terminate`）を提供。
*   **Subclassing (Win32)**: `SetWindowSubclass`によりWinUIのメッセージ処理に介入。標準的なコントロールでは困難な低レベルの入力制御（IME等）を実現。
*   **State Machine**: `StartupStage`による段階的な初期化プロセスの管理。

### 4. 改善点
*   **巨大な構造体 (God Object化)**: 1000行を超えており、タブ管理ロジック（`toggleTabViewContainer`等）が肥大化しています。これらを`tab_manager.zig`などへさらに移譲することで責務を分離できます。
*   **手動クリーンアップの脆弱性**: `fullCleanup`でのCOMリソース解放が手動であり、順序ミスによるクラッシュのリスクがあります。リソース管理用のRAII的ガードの活用範囲を広げるべきです。
*   **エラー伝播の不一致**: 一部で`log.err`後に`return`せず継続している箇所があり、異常状態での動作が不安定になる可能性があります。
*   **重複ロジック**: `setTitle`や`setControlBackground`内でのQI（QueryInterface）とキャストのパターンが重複しており、マクロやジェネリックなヘルパーで共通化の余地があります。
EXIT_CODE=0
